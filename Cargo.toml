[workspace]
members = [
    "sandart-sim",
    "sandart-render",
    "sandart-pattern",
    "sandart-wasm",
    "sandart"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
